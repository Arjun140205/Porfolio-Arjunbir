# GitHub Personal Access Token (optional - for higher API rate limits)
# Create one at: https://github.com/settings/tokens
# Only needs 'public_repo' scope for public data
REACT_APP_GITHUB_TOKEN=your_github_token_here
